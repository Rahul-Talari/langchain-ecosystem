[project]
name = "langchain-app"
version = "0.1.0"
description = "LangChain application"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipykernel",
    "langchain",
    "langchain-core",
    "langchain-community",
    "langchain-groq",
    "langsmith",
    "python-dotenv"
]